# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

def external_frameworks
    pod 'SwiftyJSON'
end

target 'Weather' do
    
    use_frameworks!
    external_frameworks
    
end
